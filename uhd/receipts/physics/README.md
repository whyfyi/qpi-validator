# uhd/receipts/physics (local outputs)

This folder holds local-only receipt outputs produced by:
- uhd/scripts/physics_corpus_validate.py

Files like:
- checksums.latest.txt
- checksums.ledger.txt

are generated locally and are NOT committed, because they depend on local PDFs.
